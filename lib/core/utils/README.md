# core/utils

Reserved for small, stateless helper functions and extensions shared across
features, per [ARCHITECTURE.md](../../../ARCHITECTURE.md).

This folder is intentionally empty for now. It's created so the folder
structure matches the architecture from the start; real helpers (e.g. date
formatting, currency formatting) will be added as the features that need them
are implemented, rather than written speculatively ahead of need.
